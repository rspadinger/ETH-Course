// SPDX-License-Identifier: MIT
pragma solidity 0.8.20;

contract ReferenceTypes { 

    //TODO Create 2 state variables: a string and a dynamic array that is initialized with the values 1, 2 and 3
    
    //TODO Create a struct called Deposit that contains 2 types for amount and timestamp
    
    //TODO Create a mapping that manages the balances for all users  
    // create a second mapping that stores only the latest deposit for each user
        
    //TODO Create a function (changeValues) that modifies the string state variable and 
    // that allows to modify the first value of the array state variable via a another locally defined array
    function changeValues(string memory newString, uint index, int newValue) external { 
    }
    
    //TODO Create a function (deposit) that allows to make a deposit and 
    // that updates the user balance and the latest deposit
    
}