// SPDX-License-Identifier: MIT
pragma solidity 0.8.20;

contract Events {

    //TODO Create a private state variable that can store the balances of thousands of addresses
    
    
    //TODO Create an event (Deposit) that will be emitted whenever a user deposits any funds. 
    // The event should log the user address, which should be searchable and the deposited amount. 


    //TODO Create a function (deposit) that allows users to deposit funds. 
    // Revert the functions if no funds are deposited. 
    // Update the user balance and emit the "Deposit" event.
    
}